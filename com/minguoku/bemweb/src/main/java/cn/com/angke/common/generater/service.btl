package ${package};
import java.util.List;
import java.util.Map;
import com.alibaba.fastjson.JSONObject;
import org.beetl.sql.core.engine.PageQuery;
import cn.com.angke.common.page.PageDto;
import cn.com.angke.${context}.entity.${entity};
/* 
* gen date ${date(),"yyyy-MM-dd"}
*/
public interface ${className} {
	void save(${entity} dto);
	PageQuery<JSONObject> pageQuery(PageDto dto);
	List<${entity}> list(Map<String,Object> queryMap);
	JSONObject find(Long id);
	void delete(Long id);

}
